/// Represents a tourism location a user can visit.
class Movies {
  final String name;
  final String imagePath;
  final int year;

  Movies(this.name, this.imagePath, this.year);

// NOTE: implementing functionality here in the next step!
}